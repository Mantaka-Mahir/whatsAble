# AutoEngage Backend Environment Configuration

# Server Configuration
NODE_ENV=development
PORT=5000

# Session Configuration
SESSION_SECRET=autoengage-secret-key-change-in-production

# Database Configuration (for future use)
# DATABASE_URL=mongodb://localhost:27017/autoengage
# DATABASE_URL=postgresql://user:password@localhost:5432/autoengage

# External Services (for future use)
# N8N_WEBHOOK_URL=http://localhost:5678/webhook
# WHATSAPP_API_KEY=your-whatsapp-api-key
# WHATSAPP_PHONE_NUMBER_ID=your-phone-number-id

# Email Configuration (for future use)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# Security
# JWT_SECRET=your-jwt-secret-for-api-auth
# CORS_ORIGIN=https://yourdomain.com
